[workspace]
members = [
    "crates/strex-core",
    "crates/strex-script",
    "crates/strex-cli",
]
resolver = "2"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
